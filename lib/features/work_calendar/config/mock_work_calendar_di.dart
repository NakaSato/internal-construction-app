/// Configure work calendar dependencies with mock implementations
void configureWorkCalendarDependencies() {
  // Note: WorkCalendarRepository and WorkCalendarBloc are now registered by Injectable system
  // All registrations are handled automatically
}
